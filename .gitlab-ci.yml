stages:
  - deploy

variables:
  APP_DIR: /var/www/wallet-backend

deploy_production:
  stage: deploy
  image: alpine:latest
  before_script:
    - apk add --no-cache openssh-client bash
    - eval $(ssh-agent -s)
    - echo "$SSH_PRIVATE_KEY" | base64 -d | tr -d '\r' | ssh-add -
    - mkdir -p ~/.ssh
    - chmod 700 ~/.ssh
    - ssh-keyscan -H $VPS_HOST >> ~/.ssh/known_hosts
  script:
    - |
      ssh -p $VPS_PORT $VPS_USER@$VPS_HOST << 'ENDSSH'
        set -e
        cd /var/www/wallet-backend
        
        echo "📦 Pulling latest code..."
        git pull origin main
        
        echo "📥 Installing dependencies..."
        npm install --production
        
        echo "🔄 Restarting application..."
        pm2 restart wallet-backend || pm2 start ecosystem.config.js
        pm2 save
        
        echo "✅ Deployment completed successfully!"
        pm2 status
      ENDSSH
  only:
    - main
  when: manual

deploy_staging:
  stage: deploy
  image: alpine:latest
  before_script:
    - apk add --no-cache openssh-client bash
    - eval $(ssh-agent -s)
    - echo "$SSH_PRIVATE_KEY" | base64 -d | tr -d '\r' | ssh-add -
    - mkdir -p ~/.ssh
    - chmod 700 ~/.ssh
    - ssh-keyscan -H $VPS_HOST >> ~/.ssh/known_hosts
  script:
    - |
      ssh -p $VPS_PORT $VPS_USER@$VPS_HOST << 'ENDSSH'
        set -e
        cd /var/www/wallet-backend
        
        echo "📦 Pulling latest code..."
        git pull origin develop
        
        echo "📥 Installing dependencies..."
        npm install
        
        echo "🔄 Restarting application..."
        pm2 restart wallet-backend || pm2 start ecosystem.config.js
        
        echo "✅ Staging deployment completed!"
      ENDSSH
  only:
    - develop
  when: manual